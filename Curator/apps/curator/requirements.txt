# Curator — runtime deps. Pip-native; lockfile lives in CI.

# --- LLM + embeddings ---
anthropic>=0.40.0
openai>=1.50.0                # used only for embeddings (text-embedding-3-small)
instructor>=1.6.0             # structured outputs from LLMs

# --- Pipeline plumbing ---
aio-pika>=9.4.0               # async RabbitMQ client (parity with asyncpg)
asyncpg>=0.29.0
pgvector>=0.3.0
boto3>=1.34.0                 # DO Spaces / MinIO

# --- API surface ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0,<3
pydantic-settings>=2.5.0

# --- Phase 2: Scrapling media fetcher (IllustrateSkill) ---
# [fetchers] extra pulls in Playwright (DynamicFetcher) + Patchright (StealthyFetcher).
# Browser binaries: `playwright install chromium && patchright install chromium` in Dockerfile.
scrapling[fetchers]>=0.2.9

# --- Utilities ---
PyYAML>=6.0.1
python-dotenv>=1.0.0
tenacity>=8.5.0
ulid-py>=1.1.0
structlog>=24.4.0
