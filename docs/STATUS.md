# InkBytes — Overall Status

> *Status: v0 live on DigitalOcean · Owner: Julian · Last updated: 2026-07-12*
> *2026-07-12: **Entity map flags (DEPLOYED).** Country entities in /entities now show a flag emoji — `lib/country-flags.ts` (ES+EN name→ISO 3166 alpha-2→regional-indicator emoji, offline, zero assets/legal risk); the browser avatar disc shows the flag for a country, the type glyph otherwise. Cities (Madrid→pin) and people (Trump→person icon) correctly keep their glyphs. Verified live (US/ES/CO/MX/AR/VE/DO/FR/CN flags; Madrid pin; People icons). **Person photos** = the parked Wikidata-P18 phase (ADR-0034 cover machinery: wbsearchentities+context re-rank+CC attribution) — a Curator enrichment, needs a go. */
> *2026-07-12: **PWA push — "Daily Outlook ready" (ADR-R-0012) DEPLOYED + verified.** Opt-in Web Push for the installed PWA. Curator owns it: migration 022 `push_subscriptions`, `push_service.py` (pywebpush + VAPID from env), `/push/{vapid,subscribe,unsubscribe,broadcast-outlook}` (last is token-guarded + internal-only). Reader: `public/sw.js` (push + notificationclick, no fetch caching), `/api/push` proxy, `notify-toggle.tsx` on /outlook (capability-gated: capable→toggle, iOS-tab→"install to enable", denied→hint). Editorial `generate_all()` triggers the broadcast after the daily batch (best-effort, token-guarded). VAPID + PUSH_TRIGGER_SECRET in infra/.env (compose passthrough). Verified live: subscribe→store→unsubscribe via the public proxy, 403-without-token + queued-with-token broadcast + dead-sub prune, vapid+sw.js served, migration applied. Real device delivery = next cron (11:59) to any real subscriber. Fixed en route: FastAPI 422 from a build_app-local Pydantic body model (hoisted to module scope). */
> *2026-07-12 (Outlook v2 — reading + save, DEPLOYED): **serif editorial typography + save-to-localStorage + citation fix.** Columns read too dense → body now long-form serif (scoped `.outlook-prose`; 18.5–19.5px/1.75 leading/airy paragraphs/accent drop cap/pull-quote; event pages untouched). **Save**: `lib/saved-outlooks.ts` (localStorage, profile later) + `OutlookSave` bookmark in the actions row + `SavedOutlooks` rail atop /outlook (persona cards, remove, self-hides). Fixed the code-span citations (LLM backtick-wraps [n]) — reader linkify swallows backticks + prompt emits plain [n]. Reader-only deploy; verified live (serif applied, save→index rail→remove, 0 citation leaks). */
> *2026-07-12: **Outlook method-persona rosters spec adopted + documented (ADR-0010, committed, backend NOT yet wired).** Julian supplied `outlook_column_personas.md` — for each of the 15 columns, a roster of ~10 journalist-METHOD archetypes (method/structure/evidence discipline, NOT identity — the spec's hard rule: names route method only, never impersonate/reach the reader) + a Global Editorial Policy + output workflow. Vendored to `Editorial/apps/editorial/prompts/personas-spec.md`; ADR-0010 documents the model. **IMPLEMENTED + DEPLOYED same day:** personas.py roster loader (16 buckets/151 personas, cached), Global Editorial Policy as a hard system preamble, LLM routing selects the method by the day's reporting problem (rotation fallback), method stored in `input_context.method_persona` (method-conditioned Phase-2 SLM data). Editorial image rebuilt (cron `59 11` now uses it) + today's 24 editions regenerated. Verified live: routing is sensible (business→Deal&boardroom, tech→Privacy&surveillance investigator, world→Human-centered conflict correspondent…), clean ES+EN, no journalist-name leakage, /outlook serves them, masthead unchanged (El Circuito/La Mesa, ADR-R-0011). Net legal-risk reduction (fact/allegation discipline).*
> *2026-07-12 (latest): **Topic folder carousel v2 — folder-definitive.svg as a live Nexora-style tile (ADR-R-0010 update) DEPLOYED + LIVE.** Iterated live with Julian to: `folder-glyph.tsx` = white folder + category-coloured border (source greys→white, strokes→accent) + soft top-right accent sheen; Nexora layout (accent icon tile TL, count TR, left-aligned name + small grey 'N stories' BL); front-folder accent glow; **staggered `tc-enter` entrance animation** (reduced-motion-safe); landscape 158×124, half the earlier vertical spend. Also this session: nested-anchor fix, /entities perf hotfix, loading skeletons + maintenance fallback, Outlook Column view + persona glyphs, video coverflow, share-branding (favicon/OG). Reader-only deploy DONE; verified live: 16 folders, tc-enter entrance animation active, sheen gradient present, fallback covered the swap (503→200).*
> *2026-07-12 (latest): **Share branding fixed — shares showed the VERCEL icon (`57f7a01`+`233a4c0`, DEPLOYED + verified).** Two-part defect: (1) `app/favicon.ico` was the untouched create-next-app default (25,931B Vercel mark, live since June 2) and the site defined NO og:image → every unfurl fell back to it. (2) `metadataBase` fell back to **https://inkbytes.app — a dead domain** (NEXT_PUBLIC_BASE_URL unset in the prod build), so absolute og/twitter URLs pointed unfurlers at a host that doesn't resolve. Fix: real favicon.ico (nib mark, 16+32 PNG-in-ICO), `apple-icon.png` 180, branded `opengraph-image.png` 1200×630 (nib mark + wordmark + tagline on ink; auto-injected sitewide), twitter card → summary_large_image, fallback domain → inkbytes.org. Verified live end-to-end: the exact og:image URL in prod HTML returns 200/32.7KB. ⚠️ Messengers cache unfurls (WhatsApp ~30d) — links shared pre-fix may show the old icon until their caches expire.*
> *2026-07-12 (later): **Outlook "Column view" redesign + persona glyphs (ADR-R-0011, `fb74f51`+`9ff1beb`) DEPLOYED + LIVE.** Bauhaus/Apple pass on the outlook pages: persona masthead (theme-accent glyph disc + localized long date + read time + 2px rule; `lib/theme-colors.ts` = single accent source), edition strip as weekday chips at the masthead, headline to 2.5rem/65ch measure, **plain-text [n] citations resolved against the timeline into real superscript event links**, actions demoted below the article, timeline as a numbered rail matching citations, "more outlooks today" snap-row (no dead ends), index persona cards. Plus **15 hand-authored monochrome persona glyphs** (La Mesa table, El Balance scale, El Pulso EKG, …, + El Editor nib fallback) — inline SVG, 24px grid, currentColor. Verified on dev vs prod data (125 discs → 15 unique glyphs; live superscript citation on /outlook/business). Dev gotcha: `.next` disk fetch-cache served a July-3 edition → lessons-learned. **Deployed 2026-07-12** (reader-only; fallback covered the swap with the branded 503). Verified live: /outlook/business 200 in 0.54s with 12 superscript citation links, edition chips, 16 persona SVGs; index carries 127 glyph discs. Entities graph cache re-warmed post-restart (15.9s once → cached).*
> *2026-07-12 (later): **Video coverflow in the event media drawer (`02069b0`) DEPLOYED + LIVE.** Julian's ask: the media-rail videos presented like the folder carousel, per a "My Videos" coverflow mockup. The drawer's flat chip row → a 3D coverflow (dark stage, violet-glow center card, angled neighbors, 16:9 thumbs + play + quality badge from width/height, swipe/chevrons/dots/keys; tap center = open video, tap side = focus). Same no-libs CSS 3D technique as ADR-R-0010; details in the ADR-R-0006 update note. Verified on dev vs prod data (Toronto 6-video event) at 375px+1280px; tsc clean. **Deployed 2026-07-12** (reader-only; the `inkbytes-fallback` page covered the swap — branded 503, no bare 404). Verified live on prod (puppeteer, Toronto 6-video event): drawer opens the coverflow, 4 cards rendered (±3 cull), 6 dots, all thumbs loaded. Entities graph cache re-warmed post-restart (1.3s).*
> *2026-07-12: **Mobile UX rework — 3D topic-folder carousel + entity browser + nested-anchor fix (ADR-R-0010) DEPLOYED + LIVE.** Julian: mobile home "confusing", asked if WebAssembly would help → answer NO (WASM can't touch the DOM; the Nexora effect is plain CSS 3D). (1) Home < sm: `topic-carousel.tsx` — gradient folder cards per live theme (count badges) on a CSS `perspective`+`rotateY/translateZ` ring; swipe rotates (rAF direct-transform, no per-frame React), settle applies the client-side category filter; chevrons/keyboard/reduced-motion; Trending collapses to an accordion row; desktop chips unchanged. (2) `/entities` < sm: entity browser (type tabs → top-10 snap cards + list + search → details bottom sheet with stats, static radial top-6-neighbor preview, coverage, re-centering chips) over the SAME /graph payload (zero backend); force graph behind "Full graph"; desktop unchanged. (3) Fixed live-prod hydration bug: nested `<a>` in card `<Link>`s — TWO instances (`AlsoIn` chips + `EventCover` CC-attribution, the latter data-dependent) → `<button>`+router.push/window.open; rule in ADR-R-0010. Verified on dev vs prod data (tunnel), 375px+1280px, tsc clean. **Deployed 2026-07-12** (reader-only surgical: image rebuilt on droplet + `up -d --no-deps inkbytes-reader`; worker/curator untouched; transient Traefik 404 self-healed). Verified live: home+entities 200; carousel markup present; AlsoIn = 22 buttons/0 anchors; credit = 2 buttons/0 anchors (two BY/BY-SA covers in card slots — the data-dependent case, now valid). **Hotfix same day (`e0f965c`, DEPLOYED):** prod /entities was unusable on phones — 24.3s TTFB (Curator /graph query costs ~16–22s; Next fetch-cache revalidates expired entries INLINE so most visits paid it) + 5.46MB HTML (full pages arrays, 1,128-row sheet). Fix: module-level SWR cache around getGraph (background refresh; 24.3s → **1.1s**) + server-side trim to 15 freshest pages/node (→ **331KB**). Curator /graph SQL optimization = flagged follow-up. **Same day: waiting cues + outage page (`6246ddd`, DEPLOYED).** loading.tsx streaming skeletons for / · /entities · /event/[id] · /outlook(+topic) — a tap now responds instantly with a branded shimmer while force-dynamic renders (the cold-graph 20s window included); friendly no-internals error states (home + entities: was 'Curator… port 8060') + Retry. Infra: `inkbytes-fallback` (nginx, one static page) — catch-all router priority=1 on the reader host + reader router pinned priority=100 + 502-504 errors middleware → deploys/outages now serve a branded auto-retrying 'We're updating' 503 (+Retry-After, noindex) instead of Traefik's bare '404 page not found'. Verified live during this deploy's own swap: fallback served the whole health-starting window, reader took over on healthy, entities warmed (1.4s).*
> *2026-07-03 (Outlook v1.1 DEPLOYED): **date-grouped timeline + Share + discoverability + a live-bug fix (ADR-0008).** (1) `/outlook` index now groups editions **by date** (newest-first timeline, "Latest" badge, per-day column count) via a new guarded `GET /outlook/archive?lang=&days=`; cards deep-link `?date=`. (2) **Share** button on the outlook page (Web Share API → copy-link fallback) beside Copy/Export/Print. (3) **Discoverability** — a home-feed Outlook promo banner + an **Outlook item in the mobile bottom nav** (sunrise `OutlookIcon`); desktop header already had it. (4) **Fix (was live-broken):** `GET /outlook?...&date=` 500'd — asyncpg needs a `date` object, not the raw string (`DataError: 'str' has no attribute 'toordinal'`), so the deployed Past-editions picker + the new grouped cards hit a broken path; now `date.fromisoformat` with malformed→latest fallback. Caught by local testing (seeded 2 dates, booted curator API + reader — verified grouped timeline / share / promo / bottom-nav render and `date=` no longer 500s). Deploy = curator-api + reader (surgical, worker untouched).*
> *2026-07-03: **"Today's [Topic] Outlook" DEPLOYED + LIVE (`inkbytes.org/outlook`, ADR-0008).** curator-api (`/outlook` + `/outlook/available`) + reader (`/outlook`, `/outlook/[topic]`, header nav, Copy/Export/Print, ES·EN toggle) rebuilt & recreated surgically (worker untouched); editorial image built on the droplet; **first batch = 30 columns** (all 15 themes × ES+EN) generated on DeepSeek and reading live; **daily cron installed** — `59 11 * * *` droplet-local (America/New_York) = 11:59 AM AST = **15:59 UTC** morning-briefing cut (also outside the DeepSeek peak window, ADR-0038) → committed wrapper `infra/run-editorial.sh` (reads infra/.env; provider stays a flag → flip to local gemma4 after the 16 GB upgrade, env-only). EN generation validated on prod (clean English, no Spanish bleed). Persona shown as display name ("El Circuito"), not the kebab key. Verified live: `/outlook/technology` ES+EN 200 with content, index lists El Balance/El Circuito/La Mesa. NEXT: Phase 2 SLM distillation (the `editorials` table's input_context+prompt = the training set).*
> *2026-06-28: **"Today's [Topic] Outlook" — daily editorial as a reader page (ADR-0008, committed, NOT deployed).** The editorial (Editorial service, ADR-0008) surfaced as a product: per-topic daily column, **bilingual ES+EN**, **morning-briefing cut 11:59 AM** (edition covers prior ~24h; `editorials` unique-per-day = saved archive). Curator API `/outlook?theme=&lang=&date=` (edition + cited-events **timeline** + archive dates) + `/outlook/available` (guarded → empty until the service runs). Reader `/outlook/[topic]` (persona header + editorial + timeline + **Copy / Export .md / Print** + ES/EN toggle + date picker) + `/outlook` index + header nav. Editorial `Dockerfile` (one-shot batch for a daily cron). tsc + AST clean. **Deploy = curator-api + reader + build editorial image + a 11:59 cron (docker run) + a first manual run to populate.** Editorial gen validated earlier on prod (El Atlas ES column); EN pass pending validation. Phase 2 = distill editorials → SLM.*
> *2026-06-28: **ADR-0034 — Unsplash added as a cover source (committed, NOT deployed).** `cover_image.unsplash_cover()` via the official Unsplash API (commercial-OK Unsplash License); `pick_cover` order now Wikimedia-canonical → Unsplash → Openverse CC0 → procedural. Gated on `UNSPLASH_ACCESS_KEY` (blank → skipped; verified the chain still resolves via Wikimedia/Openverse without it). ToS honored (photographer+Unsplash credit via the cover overlay + download_location trigger); kept generic (theme/entity keyword), never event footage. **To activate: register at unsplash.com/developers → `UNSPLASH_ACCESS_KEY` in infra/.env** (worker compose passthrough added). Used the API, NOT the search-page URL (scraping violates ToS).*
> *2026-06-28: **Editorial service — Phase 1 scaffold (ADR-0008, committed, NOT deployed).** New standalone service `Editorial/apps/editorial/` (separate from Curator per ADR-0008): daily per-theme editorial columns (~450–600w, named persona voice per vertical — La Mesa/El Balance/El Pulso/…, inline `[n]` citations). Provider-pluggable LLM (ollama+gemma4 / deepseek / anthropic; ~12B is the bake-off quality floor). Reads Curator's published pages, writes an `editorials` table whose rows carry `input_context`+`prompt` → it's also the **training set for the Phase-2 distilled editorial SLM** (the user's cost-at-scale/local goal). Scaffold = config/db/llm/personas/prompt/orchestrator/CLI/migration; plumbing validated on dev (migration applies, min-events gate works). Next: validate generation on prod data, Reader "Editorial" surface, daily cron + Dockerfile/compose, then Phase 2 bulk-distill → LoRA 2–3B → GGUF on Hostinger. SLM is Phase 2 (15 editorials/day ≈ $0 already; the SLM is a speed/scale play + the data factory is Phase 1).*
> *2026-06-28: **ADR-0034 P1b — Wikimedia/Wikidata covers + cover AGENT (committed, NOT deployed).** Hero images now come from **Wikimedia Commons** when possible: `cover_image.wikimedia_entity_cover()` resolves the event's top LOC/ORG entity → Wikidata Q-id → P18 image → Commons imageinfo, license-gated (accept CC0/PD/BY/BY-SA; reject NC/ND/NonFree/Restrictions; store Artist attribution for BY/BY-SA). `pick_cover()` orchestrates Wikimedia-canonical → Openverse CC0 → procedural. Canonical images are far more relevant (Argentina→Fitz Roy, Madrid→cityscape, FIFA→HQ). **The agent: `Application._cover_safe` runs fire-and-forget after every synthesis** (beside IllustrateSkill) — new events auto-get a Commons cover; backfill_covers.py (now pick_cover-based) covers the backlog. Reader renders a BY/BY-SA credit overlay. Validated live (Wikidata→P18→Commons returns clean-license images; pick_cover Wikimedia-first works). Deploy = curator(api+worker)+reader, then backfill. Pending: Tier 3 AI.*
> *2026-06-28: **ADR-0034 P1a — license-clean generic covers via Openverse (committed, NOT deployed).** Builds on P0: where the procedural cover was the only option, now fetch a commercially-licensed CC0/PDM generic image keyed to the event's top LOC entity (place → landmark) or theme (crime→courthouse, business→trading floor). `services/cover_image.py` (Openverse, `license_type=commercial`+`license=cc0,pdm`, deterministic pick, fail-open); migration 021 `events.cover_image` JSONB (provenance); `scripts/backfill_covers.py` (rate-limited, dry-run default); `/events`+`/events/{id}` return it; Reader `components/event-cover.tsx` layers the image over the procedural cover (onError → procedural). Validated live vs Openverse (CC0 landmarks/courthouses/stadiums). CC0/PD only → zero attribution burden. Pending: Wikimedia/Wikidata P18 entity path + by/by-sa attribution + Tier 3 AI. Deploy = curator(api+worker, migration auto-applies)+reader, then run the backfill.*
> *2026-06-28: **ADR-0034 P0 — owned procedural event covers (committed, NOT deployed).** Heroes/cards were rendering source outlet `og:image`s — including outlet brand LOGOS (infobae's orange card) — which is legal risk L3 (unlicensed wire photos, launch-gate M1). Built Tier 1: `Reader/components/procedural-cover.tsx` = deterministic owned cover (seed=event id → theme-color gradient + seeded blobs + category glyph). Now the hero (event page) + feed Lead/Secondary cards always show the owned cover; source og:image no longer rendered (still stored), and dropped from the OG/Twitter share meta too. `/events/{id}` returns majority `theme` → cover color. Kills the logo + the L3 exposure in one move. P1 (CC0/Openverse + Wikidata generic photos for variety) is the chosen follow-up. tsc + AST clean.*
> *2026-06-28: **Cross-language event dedup — collapse + link (ADR-0037, committed, NOT deployed).** Same story in EN+ES showed as two feed cards ("3 Venezuela entries"). bge-m3 makes them trivially detectable (centroid distance 0.03–0.06 on prod). Chose collapse-not-merge (keep clean per-language pages). `/events` runs one bounded self-join (pgvector `<=>`, gated on the lifecycle window) tagging each event `primary` (false when a richer other-language sibling exists) + `also_languages` `{lang: page_id}`. Reader "All" view shows only `primary` (one card/story) + "also EN/ES" chips linking to the sibling; EN/ES tabs unchanged (no story lost). Validated read-only on prod: richer-language version is primary, dups collapse. tsc + AST clean. Curator/docs/adr/0037.*
> *2026-06-28: **ADR-0035 evolving titles (P0+P1) + synthesis max_tokens fix (committed, NOT deployed).** (1) Title history: migration 020 `pages.title_history` JSONB; `SynthesizeSkill._persist` appends the prior headline (+publish time +source count) on re-synthesis only when it changed (dev-validated: no dup on unchanged); `/events/{id}` decodes it; event page renders a collapsible "Story timeline · N earlier headlines" (empty until stories re-synthesize with evolved titles). (2) `max_tokens_synth` 2500→8000 (config default) — 2500 truncated ~5% of large clusters ("output incomplete"); deepseek-reasoner competes thinking vs output tokens, 8000 is its answer ceiling. ⚠️ live value is the Backoffice `curator_settings.max_tokens_synth` overlay (prod=2500) — must UPDATE it to 8000 (worker polls ~30s, no redeploy) + re-run `--synthesize-pending --since-hours 96` to recover the ~114 failed events. Cross-language dedup still pending.*
> *2026-06-28 (later): **Umami analytics LIVE on prod.** DNS `analytics.inkbytes.org` → droplet resolves + LE cert issued (200); `inkbytes-umami` container healthy (19 tables migrated in the `umami` DB on the shared Postgres); website created (id `10473a53-7770-47f7-9c06-48f2e61bee48`); reader rebuilt+recreated with `UMAMI_SRC`+`UMAMI_WEBSITE_ID` → website-id confirmed in live inkbytes.org HTML, `script.js` served 200. Pageviews + custom events (`event-read`, `read-depth`) register on real visits (JS-less bots/curl don't inflate). Deployed surgically (umami + reader only; worker untouched). ⚠️ **OUTSTANDING: change the umami admin password** — dashboard is public with default `admin`/`umami`. Runbook: `docs/analytics.md`.*
> *2026-06-28: **Visitor + content analytics — self-hosted Umami (built, committed, NOT deployed).** Privacy-first/cookieless (no consent banner) — fits the ad-free brand + GDPR/LATAM-EU audience + the legal-risk memo; data stays on the droplet (separate `umami` DB on the existing Postgres). Chose Umami self-host over Plausible-self-host (needs ClickHouse, too heavy) + over GA4 (cookies/consent/ad-tech). New `inkbytes-umami` service (`docker-compose.prod.yml` + Traefik labels in `docker-compose.do.yml` → `analytics.inkbytes.org`). Reader: `components/analytics.tsx` (runtime-env script loader → set website-id with NO rebuild) + `components/read-tracker.tsx` content-engagement custom events (`event-read`, `read-depth` 25/50/75/100%) on the event page. tsc + compose-YAML clean. **Manual steps to go live (see `docs/analytics.md`): DNS A record `analytics.inkbytes.org` → droplet; `UMAMI_APP_SECRET` + `CREATE DATABASE umami`; deploy umami; first-run (change admin pw, add website → get UUID); set `UMAMI_SRC`+`UMAMI_WEBSITE_ID` in infra/.env → recreate reader.***
> *2026-06-27: **Feed card timestamp now shows the "last update" clock (freshness_at), not occurred_at; event page shows BOTH started + updated (Reader + Curator API, committed, NOT deployed).** UI bug: feed cards displayed `occurred_at` ("5d ago") while the event page showed "Updated 6h ago · DEVELOPING" — so an actively-developing story (Ukraine: occurred 5.7d, freshness 6.5h) read as stale "5d ago" at the top of "most recent". Fix: card timestamp + DEVELOPING badge + sort all route through `lastUpdate(ev)=freshness_at` (`feed-client.tsx`) — matches the event page's "Updated". Chose `freshness_at` over `last_material_update_at` after live data showed last_material is **processing-contaminated** post-reset (uniformly ~0.1h because the pipeline clusters backlogged articles NOW, even when the newest article is 1.5d old); freshness_at is the honest last-news time, and tight clustering (ADR-0031) now prevents the off-topic float that ADR-0033 used the material clock to avoid. Event page (`event/[id]/page.tsx`) now renders "Started {occurred_at} · Updated {freshness_at}" — `/events/{id}` SELECT gains `e.first_seen_at AS occurred_at`. tsc + AST clean. **Pending curator-api + reader deploy.***
> *2026-06-26: **Entity graph "People = 1" fixed — it was the /graph node SELECTION, not NER (ADR-0036, committed, NOT yet deployed).** Investigation disproved the "extraction broken" assumption: the prod `entities` table is rich (91,385 PERSON mentions / 34,850 distinct, avg 12.1 ents/article, 186 distinct people in the top event). The bug: `/graph` ranked nodes purely by `event_count DESC LIMIT 80`, so ubiquitous countries (estados unidos 402, españa 279, méxico 265…) + wire services (efe, cnn) crowded out people → only Trump survived. Fix (api_server.py): per-type quotas (PERSON ~45% / ORG ~30% / LOC ~18% / EVENT ~7%), drop wire-service/outlet bylines (`_GRAPH_SOURCE_STOPWORDS`), dominant type from RAW mentions (was an alphabetical collapse that demoted PERSON). Validated read-only on prod: PERSON 8→**35**, surfaced real newsmakers (Trump, Sánchez, Messi, Sheinbaum, De la Espriella, Petro, Marco Rubio, EU). Local API boots + /graph 200. Enabling dormant spaCy NER would have been wasted effort. Follow-up: cross-language entity dup (estados unidos/united states) bounded by the quota but not merged. **Pending deploy.***
> *2026-06-25 (later): **CLEAN-SLATE "keep recent" RESET on prod + clustering retuned to absolute specificity.** Chosen over re-clustering ~104 legacy mega-buckets (bigger/irreversible; legacy corpus is mostly outside the freshness window anyway). Discovered that "start fresh" ALONE doesn't fix it — re-clustering a clean corpus from scratch STILL rebuilt the World-Cup blob (363 on dev, **421 on prod**). Two clustering fixes (commit `022b8a1`, deployed): (1) `specificity_min_shared=2` (was implicitly ≥1 — a big cluster's accumulating specific-entity union was a magnet); (2) **`specificity_cap` (fraction of pool) → absolute `specificity_max_df=90`** — the fraction admitted ~1,261-article entities as "specific" at n=25k, re-merging team names; an absolute df threshold is window-independent. `precision_distance 0.48→0.30`. Live params: `PRECISION_DISTANCE=0.30, SPECIFICITY_MAX_DF=90, SPECIFICITY_MIN_SHARED=2`. Validated: prod top clusters all coherent real events (Venezuela quake, France/Lecornu, Portugal–Uzbekistán, Mexico WC, UK Labour) — no blob. Reset runbook: `pg_dump` backup (781MB, `/opt/inkbytes/backups/`) → wiped events/pages (FK cascade) + articles <72h → kept **26,052 articles (72h)** → `recluster_fresh.py --apply` (0.30/90/2; vectorized BLAS clusterer `470e9d5`; tuned LOCALLY on an exported gz since in-container BLAS is ~15min/run) → `--synthesize-pending --since-hours 96` (richest-first). content_filter gains a live-blog/date-only publish gate. See ADR-0031 Update 2026-06-25.*
> *2026-06-25: **ADR-0031 precision clustering DEPLOYED + ENABLED on prod.** Deployed dormant (migration 019 `events.centroid`), backfilled 19,095 event centroids, then `infra/.env`: `PRECISION_MODE=true`, `PRECISION_DISTANCE=0.48`, `SPECIFICITY_CAP=0.05` (compose passthrough commit `8dab5b4`). Verified LIVE: `CLUSTER-P attach` on real Venezuela articles (centroid_d 0.06–0.12, spec 21–28 entities, correct attaches), worker healthy, no errors. Effect: **new clustering uses centroid-linkage + entity-specificity → no new mega-buckets**. ⚠️ The ~105 EXISTING mega-buckets (the "Mexico leads Group A · 18d" screenshot) PERSIST until re-clustered — `scripts/recluster_megabuckets.py --apply` (dry-run-validated; cap 0.03 splits best; needs re-synth of the resulting events) is the deliberate follow-up. **Revert:** `PRECISION_MODE=false` in `infra/.env` + recreate curator-worker. Prior P0 lifecycle (ADR-0033) still enabled.*
> *2026-06-25: **ADR-0031 clustering precision BUILT (committed `6d94ef0`, DORMANT, not deployed).** The fix for World-Cup mega-buckets (one event = 2,085 articles / 18 days — the "18d ago in Latest" the lifecycle work exposed). Behind `clustering.precision_mode` (default off → legacy single-linkage untouched): migration 019 `events.centroid`; `cluster.py::_run_precision` = centroid-linkage (distance < 0.48) + entity-specificity gate (share ≥1 entity in ≤5% of the recent pool) + staleness gate + shared breaking helper; `scripts/backfill_event_centroids.py` (run before flipping). Verified live (dev): a WC article with a specific entity attaches (d=0.296); the same embedding with only ubiquitous "mundial 2026" SEEDS — the gate blocks the mega-bucket merge. **Enable:** backfill centroids → add the 3 flags to compose passthrough (like LIFECYCLE_FEED) + `PRECISION_MODE=true` in `infra/.env`; then re-cluster the ~308 legacy mega-buckets. P0 lifecycle (ADR-0033) is already enabled on prod.*
> *2026-06-25: **ADR-0033 P0 event lifecycle DEPLOYED + ENABLED on prod.** Deployed dormant (migration 018 `events.last_material_update_at`, backfilled 18,846 rows) then activated via `infra/.env`: `LIFECYCLE_FEED=true`, `FEED_WINDOW_HOURS=72`, `MATERIAL_MAX_DISTANCE=0.40` (compose env passthrough added to both curator services — commit 33d1d00). `CONCLUDE_AFTER_DAYS` held at 0 (archiving = the only data mutation, deliberate follow-up). Effect: the `/events` feed now ranks by the material clock + a 72h window → draws from **1,353** windowed events vs **4,219** published (excludes ~2,866 with no 72h activity); Reader shows `occurred_at` as the date (9-day-old story reads "9d ago"). Verified: feed 200, no errors, `CLUSTER attach … material/tangential` tagging live, inkbytes.org 200. NOTE: material clock was backfilled=last_updated_at, so the resurfacing-specific exclusion accrues GOING FORWARD as tangential attaches stop bumping the material clock; the immediate win is the stale-event window. **Revert:** remove the 3 flags from `infra/.env` + `compose up -d inkbytes-curator-api inkbytes-curator-worker` (instant, no rebuild). P1/P2 + conclude/archiving pending.*
> *2026-06-25: **Video rail outage fixed — Chromium PID-leak (extends ADR-0011).** Prod showed no YouTube/video links: IllustrateSkill's headless Chromium had leaked **~9,367 orphaned `chrome-headless-shell` processes** in the curator-worker (PID 1 = bare python is not a subreaper), maxing the container PID table (`pids.current 9379 / pids.max 9388`) → `pthread_create EAGAIN` → every launch `SIGTRAP`ed → 0 video items since ~06-21. NOT seccomp (it was applied) and NOT the chromium-148 version/flags (all fine once PIDs freed). **Immediate fix: `docker restart inkbytes-curator-worker`** (pids.current 9379→7, host chrome procs 9371→4; chromium launches again → rail repopulates next batch synthesis). **Durable fix DEPLOYED 2026-06-25** (commit `484372c`): `init: true` (tini reaps zombies) + `pids_limit: 4096` (blast-radius cap — was unbounded) on the worker in `docker-compose.do.yml`. Applied **surgically** (worker-only `compose up -d`, not full `--build`, to avoid activating other commits on master); verified live: `Init=true PidsLimit=4096`, PID 1 = `docker-init`, chromium launches OK, other services untouched. Follow-up idea: replace the headless-browser YouTube scrape with the YouTube Data API to drop the Chromium dependency entirely (3rd Chromium-in-Docker incident).*
> *2026-06-16: **Flood / DDoS hardening (ADR-0009, committed not deployed) — layers #2+#3.** Audited posture was wide open (no rate limiting anywhere, no CDN). Added: (#2) Traefik per-IP `rateLimit`+`inFlightReq` on the Reader (100/s burst 200, inflight 100) and Backoffice (30/s burst 60, inflight 40) routers — `inkbytes-`-prefixed middleware in `docker-compose.do.yml`, limits by spoof-resistant RemoteAddr; (#3) an in-memory limiter on the LLM-backed `POST /api/ask` (per-IP 8/60s + global 40/60s → 429, runs BEFORE the LLM call — kills cost-amplification floods) and `throttle:10,1` on the Backoffice login POST (atop Breeze's per-credential lockout). Verified locally: Reader `tsc` clean, limiter algorithm test (blocks at req #9, global cap at 40, window expiry), `route:list` shows the login throttle, Traefik labels parse in the merged compose. **Deferred (owner choice):** #1 Cloudflare proxy (the big anti-volumetric win — needs DNS migration) + #4 fail2ban/firewall. Not yet deployed.*
> *2026-06-16: **ADR-0032 Phase 1 DEPLOYED to prod** (DO droplet, `deploy.sh --build` from `/opt/inkbytes`, profile `do`). Live + verified: item-1 taxonomy (15 themes + 33-cat `article_category`) writing on freshly-enriched articles (e.g. `disaster→Disaster & Emergency`, `crime→Crime & Justice`, `health→Science & Technology`); new themes already appearing; `/events?theme=…&category=…` filters 200; Reader 15-theme chips + Backoffice filter (`87cbb47`) live; `inkbytes.org` + `admin.inkbytes.org` 200. **NER pre-pass (item 2) is deployed but DORMANT** — `NER_ENABLED` unset + models not baked (`INSTALL_NER_MODELS=false`); worker steady at ~140 MB, no spaCy RAM. Cost steady (~$0.40/1000, 33-cat list cached). spaCy lib now in the curator image (installs cleanly). **Phase 2 (enable NER) NOT done** — requires editing `deploy.sh` to pass `--build-arg INSTALL_NER_MODELS=true` + `NER_ENABLED=1`, then watch worker memory (md pair ≈1.0 GB on the 1.5 GB cap).*
> *2026-06-15: **ADR-0032 item 2 — spaCy NER pre-pass (committed, not deployed; ships DISABLED).** A language-routed, deterministic NER pass (`services/ner_prepass.py`) runs after triage / before enrich and feeds the LLM typed `MUST_COVER` entities (PERSON/ORG/LOC) so it can't under-extract; EVENT stays the LLM's job. Multilingual (EN OntoNotes + ES/`xx` CoNLL → normalized), closing the legacy Entopics EN-only gap. spaCy runs in a worker thread under a timeout, bounded by `_ner_sem`; **fail-open everywhere** (gated off / unsupported lang / missing model / load error / timeout → LLM-only). **Two gates, both default-off:** build-time `INSTALL_NER_MODELS` keeps the ~600MB models out of the image; runtime `NER_ENABLED` keeps the pass dormant. `NerCfg` defaults to **memory-safe** models (`en_core_web_md`/`es_core_news_md`), per-lang env overrides. `scripts/test_ner_prepass.py` 15/15 green + real-LLM `--dry-run` proof (Spanish fixture → typed entities the LLM covered). **Roll out like triage** (bake models → enable one cycle → watch worker memory). Measured peak RSS picked the defaults: md pair ≈0.69 GB (fits the 1.5 GB worker), lg pair ≈1.24 GB (breaches the cap under a concurrent Chromium illustrate run); `exclude=vectors` crashes the es models so lg can't be slimmed. Opt up to `es_core_news_lg` (`NER_ES_MODEL` + bake it) only on a ≥16GB box. ADR-0032 items 1+2 done; items 3–4 (Wikipedia linker, 346k-corpus archive) not built.*
> *2026-06-15: **ADR-0032 item 1 — taxonomy widened (committed, not deployed).** The salvaged IPTC-grounded ontology is now wired into Curator's enrich path: `theme` widened **8→15** (adds science/entertainment/crime/education/lifestyle/religion/disaster; the old 8 are a strict subset so legacy rows + `?theme=` chips stay valid), plus a granular **33-category** `article_category` the LLM picks (hinted by the 634→33 IPTC/HuffPost/20NG bridge over Messor's section/tags) and the bridge as deterministic fallback on abstain. New `services/taxonomy.py` loader (`CATEGORIES_33` / `normalize_category` / `suggest_category`); `article_category` persisted via `COALESCE` (non-destructive — existing rows upgrade as they re-enrich); `_VALID_THEMES` widened; `scripts/test_taxonomy_widen.py` (17 checks, all green) + real-LLM `--dry-run` verified. **Reader/Backoffice still show the original 8 theme chips** — the 7 new themes flow through safely (no crash) but lack a dedicated chip/icon/filter until the UI is widened (immediate non-breaking follow-up). ADR-0032 → accepted. Items 2–4 (spaCy NER pre-pass, Wikipedia linker, 346k-corpus archive) not built.*
> *2026-06-14: **inkbytes.news marketing site (WordPress) — v2 multi-page theme.** Public marketing site live at `inkbytes.news` — WordPress on the Hostinger box (`82.112.250.139`, `/docker/wordpress-yybr`, behind Traefik + LE cert), NOT the droplet. (1) Security-hardened (additive `docker-compose.override.yml` + mu-plugin): proxy-aware HTTPS, `DISALLOW_FILE_EDIT`, XML-RPC blocked at proxy, REST/`?author=` user-enumeration killed, HSTS+security headers, `/wp-login.php` rate-limited. (2) The `inkbytes` theme is now a **full multi-page v2** built from the design-system static site `docs/marketing/site/`: home + how-it-works + why + features + pricing + methodology + about + contact + privacy + terms + The Desk (index + 1 post) + 404 — all verified live (HTTP 200, zero PHP errors). The site's `_ds/` design-system bundle isn't vendored, so the theme is **self-contained**: DS base tokens reconstructed in `style.css` (Archivo/Spectral/IBM Plex Mono), the DS React widgets rendered as **static HTML** (`parts/widget-*.php`), chrome server-rendered from `chrome.jsx`. No runtime React/Babel. Source of record: `docs/marketing/inkbytes-theme/` (`build-templates.py` regenerates page templates from `site/*.html`; `seed-pages.php` creates the Pages + pretty permalinks). Deployed **manually** to Hostinger via scp+docker cp (strip macOS quarantine xattr first). **Spanish `/es/` pages deferred** (needs Polylang). CTAs → reader at inkbytes.org. See memory `hostinger-wordpress-hardening`.*
> *2026-06-14: **Session batch (cost accuracy, taxonomy nav, pulse fix, entity coverage, bot icon).** (1) Cost-meter accuracy (Curator ADR-0028) — was overstating ~15× (reported ~$62/day; real DeepSeek invoice ~$3-4/day). Root cause: Claude-Haiku list prices ($1/$5 per Mtok) instead of deepseek-v4-flash ($0.14/$0.28) + ignoring the cache-hit discount ($0.0028/Mtok, ~half of input is cached). Now splits input cache-hit/miss; COST log shows `cache_hit=`; verified to the cent against the invoice. Live + confirmed. (2) Taxonomy navigation (Curator ADR-0027) — `/events?theme=&topic=&category=` filters + `/topics/trending`; Reader theme chips (pre-existing) + new Trending strip with server-side `?topic=` drill-down. Trending **recency-weighted** (hotness = coverage / (age+2)^1.2) so fresh topics lead the carousel instead of sinking to the end. (3) Pulse-lane tagging fix — `publish_scrape_session_completed` dropped `lane` from the payload, so every pulse session arrived untagged → Backoffice pulse filter was empty despite pulse running (8 outlets/5min). One-line forward fix. (4) Entity extraction — ENRICH prompt now defines all 5 types (PERSON/ORG/LOC/EVENT/OTHER) and requires typed coverage; EVENT was under-extracted (3.7% of entities) and mis-typed as OTHER (deals/negotiations) — verified fix on real samples. (5) Reader floating chat button → bot icon. (6) NewsScraper logs now under `Inkbytes.Messor` so per-stage breakdown (stale-skip/parse-fail/lang-skip) is visible — exposed that "0-success" outlets are German (lang-skip), paywalled (parse-fail), or undated (stale-skip), NOT lack of news. anthropic outlet disabled (no date, no feed). All deployed 2026-06-14.*
> *2026-06-13: **Pre-enrich triage gate** (Curator ADR-0030) — a small local model (`llama3.2:3b` on the Hostinger box) classifies each to-be-enriched article keep|junk from title+lede BEFORE the paid DeepSeek enrich, dropping filler (horoscopes/lottery/deals/dead pages). Runs after the dedup fast-path, before enrich; fail-OPEN (timeout/error → keep); bounded to 2 concurrent. Shipped in **shadow mode** (logs `TRIAGE shadow-DROP` without dropping) to measure drop-rate + precision before enforcing. First shadow batch caught `qwen2.5:3b` mislabeling real news → switched to `llama3.2:3b` + dropped the sports-betting/fixture categories (3B can't tell a match preview from a betting tip; betting is already gated at synthesis). Bigger models (mistral:7b 81s, gemma4:12b 500) too slow on the CPU box. **Now ENFORCING** (`shadow=false`) — shadow ran clean (50 verdicts, 2 drops, 0 false positives, 0 thrash errors), so junk is dropped before enrich as of 2026-06-13. Drop rate ~4% (lower than the 15-25% estimate after removing betting). Revert = set `TRIAGE_SHADOW=true` in droplet `infra/.env`. See ADR-0030 addendum. **⚠️ Known issue:** dropped articles have no terminal marker (`topic`/`enriched_at` stay NULL), so `--reenrich-missing` would re-enrich them and resurrect the junk — **do NOT run `--reenrich-missing` while triage is enforcing** (disable triage first). Also makes the dashboard "pending enrichment" counter drift upward. Fix flagged (add `triage_dropped_at` column), not yet built — ADR-0030 "Known limitation".*
> *2026-06-13: **Outlet redundancy analysis + "Decisive" column** — measured which outlets are near-duplicative (co-occurrence Jaccard + a "decisive %" = share of an outlet's events where it's load-bearing for the ≥2-source bar). Safe-cut signal now a live column on the Backoffice Outlets screen (red <25%). Findings: polifact (3%, never decisive) + mileniomx (subsumed by excelsiormx) are the clearest cuts; redundancy is within same region+language only. Full writeup: `docs/outlet-redundancy-analysis.md`.*
> *2026-06-13: **Rich markdown news body** (Reader ADR-0009) — event body now renders `synthesis_md` via react-markdown + remark-gfm (headings, bold, lists, blockquotes, links, tables) with brand-styled components + `[Source:]` → inline `cite` chips (rehype pass; raw HTML not rendered). synthesize.md prompt now asks for STRUCTURED markdown (bold lead → `##` sections → key-point bullets, 220–320w). Plus a feed hydration fix: relative timestamps routed through `<TimeAgo suppressHydrationWarning>` (Date.now() server/client drift was regenerating the tree). Existing plain pages still render; richer structure lands as they re-synthesize.*
> *2026-06-13: **On-Demand Breaking Desk shipped** (Curator ADR-0029, 3 services) — Backoffice screen: enter a title (Google News RSS search → batchexecute URL decode) or paste a URL → Messor scrapes on demand at AMQP priority 9, freshness gate bypassed → editor marks the resulting event breaking / clears / force-publishes (synthesize.run(force=True) past the ≥2-source + promo/noise gates). New Messor `core/gnews_search.py` + `POST /api/scrape/on-demand`; Curator `event.mark_breaking`/`clear_breaking`/`force_publish` commands; Backoffice "Breaking Desk" page + CuratorCommandService methods. Verified locally: search decoded 5/5 real URLs incl. the exact example; URL-mode scraped anthropic.com/news/fable-mythos-access (freshness bypass) and published @ priority 9; screen renders + Vite build compiles. Committed locally, NOT pushed/deployed.*
> *Also confirmed live (local): the ADR-0028 cache-aware cost meter — COST lines now show `cache_hit=` and project ~$0.27/1000 articles.*
> *2026-06-12: morning maintenance — REINDEX embedding index (recall + space); embed concurrency benchmarked → keep `_embed_sem=1` (Hostinger bge-m3 CPU-bound ~19/min, concurrency thrashes, ADR-0026 addendum); cost persistence confirmed healthy (was a query-schema mistake). Overnight: queue drained 3,700→~400, lag 10h→~30min.*
> *2026-06-12: **Ink-shader splash background** (Reader ADR-0008) — the daily "Morning Briefing" splash now renders over an animated WebGL ink/smoke field (vendored InkWall engine). Brand palette: deep navy + luminous indigo primary ink + a second coral (`--accent-dot`) ink stream drifting on an independent path. Everyone gets a gentle slow drift (reduced-motion → slower, never a frozen frame); WebGL-unavailable falls back to solid `--accent`. Drag-to-stir. Committed locally.*
> *2026-06-12: **Taxonomy navigation shipped** (Curator ADR-0027, all 3 surfaces) — 6a Curator API: `/events?theme=/?category=/?topic=` filters + `/topics/trending` (junk-excluded, page-aligned counts); 6b Reader: trending-topic strip + `?topic=` server-filtered drill-down (shareable URL, count==result); 6c Backoffice: theme/category/topic filters on the moderation view. Makes the previously-idle taxonomy indexes earn their keep. Committed locally, NOT yet pushed/deployed.*
> *Pipeline proven end-to-end. 413+ published pages. 22 active outlets. Continuous 12×/day harvest cycle.*
> *2026-06-08: ADR-0015 (synthesis cost cap + dedup fast-path) + Messor ADR-0012 (persistent staging volume) deployed — restart-driven queue flood eliminated.*
> *2026-06-08: ADR-0018 — `content_hash` made stable (normalized lede prefix); fixes the ADR-0015 fast-path that never fired. Tested locally, pending deploy.*
> *2026-06-09: 105 143-msg backlog purged. Root cause = ADR-0016 migration teardown wiped the dedup volume + per-DAY staging files re-published in full every cycle. Fixed by Messor ADR-0014 (per-RUN staging files). Tested locally, pending deploy.*
> *2026-06-09: Messor ADR-0015 — 48h harvest freshness gate (strict on undated) + dropped the archive tail-crawl. Stops month-old (back to 2012) articles entering as "fresh". History preserved (no prune). Tested locally, pending deploy.*
> *2026-06-09: Reader ADR-0007 — mobile daily splash ("Morning Briefing"), once per 24h over the home feed. Real Reader design system (Inter, LogoMark, --accent/--accent-dot); real local streak + live 24h category counts. Tested locally.*
> *2026-06-08: Curator ADR-0019 — lead-image hotlink guard. Outlet og:images embedded as a cross-origin `<img>` get hotlink-redirected by some CDNs (brightspot/LA Times) to a 1×1 `placeholder-1x1.png` (HTTP 200) → blank hero with no `onError`. `MediaValidator` probes each og:image at ingest with the browser `Sec-Fetch-*` fingerprint and NULLs blocked ones so the `/events` rollup falls back to another source. **Deployed + backfilled** (293 rows / 217 blocked URLs across 4,683 distinct). Verified live: the reported event now renders The Guardian's real photo. Two follow-up fixes: skip non-http(s) URLs (`data:` URIs were crashing the probe + would crash live ingest); don't false-positive images served without a Content-Type.*
> *2026-06-08: Messor — removed dead `process_found_articles` (no callers; pre-ADR-0011 thread-pool + ADR-0015-removed head+tail slicing). On branch `chore/remove-dead-process-found-articles`, **not yet pushed**.*
> *2026-06-09: Curator ADR-0021 — lead-image must come from a cluster CORE member. A marginal/mis-clustered article (e.g. a Stevie Nicks/Fleetwood Mac photo at distance 0.47 on the "Michael Tilson Thomas, conductor, dies" obituary) was hijacking the hero because it was the freshest article with an image. The `/events` rollup now gates `cluster_distance <= 0.45` (attach threshold is 0.50); events whose only image is an outlier go text-only. Query-time rollup — no migration. Impact: 281 events → 270 keep hero, 8 go text-only, 39 switch to a more-core image.*
> *2026-06-09: Curator ADR-0020 — promotional/commerce content filter. Affiliate clusters (e.g. The Guardian's shopping vertical → "Guardian Covers Top New Mom Gifts and Sonos Speaker Reviews") no longer publish: SynthesizeSkill skips a cluster whose article titles are a strict majority commercial, and refuses ad-style synthesized headlines. Deterministic `services/promo_filter.py`, tuned to 1/282 page headlines + 17/4000 article titles (zero false positives). Backfill un-publishes existing ad pages (`scripts/backfill_promo_pages.py`).*
> *2026-06-10: Messor ADR-0013 (RSS-first harvesting) **implemented** — `feed_url` per outlet in DB (Curator migration 012 + Laravel migration 000001); `FeedScraper` / `get_articles_from_feed()` with 48h freshness gate + newspaper3k fallback; BBC, Al Jazeera, Reuters + 4 LATAM outlets seeded. Messor ADR-0016 — per-outlet scraper config pattern (`min_word_count`, Curator migration 013). Logger level bug fixed (INFO was silently dropped — `LoggingService` never called `logger.setLevel()`). arm64 architecture fix for Backoffice-triggered scrapes confirmed working. Deployed 2026-06-10.*
> *2026-06-11: **prod `feed_url` backfill + stub bug found** — the 2026-06-10 RSS seeding only landed in `outlets.json` (local fallback); prod reads outlets from Curator API → DB where `feed_url` was NULL for all 65 outlets, so RSS-first was dormant. Every candidate feed probed from the droplet with a browser UA; 22 verified-working feeds + 9 `min_word_count` overrides backfilled via psql. Corrected 7 dead/wrong URLs (Arc Publishing `arc/outboundfeeds/rss/?outputType=xml` for infobae/lanacionar/eluniversalmx/semana; real CNBC feed; economist `/latest/rss.xml`; eltiempo `colombia.xml`). No working feed: apnews, cnn, reuters, elespectador, animalpolitico, mileniomx — stay on newspaper3k. First live BBC run then exposed TWO blockers, both fixed + deployed same day: (1) `_FeedArticleStub` lacked `.download()` — every feed article died on AttributeError and the newspaper3k fallback never fires when the feed fetch itself succeeds; fixed by returning real undownloaded `newspaper.Article` objects. (2) newspaper3k can't extract a byline date from BBC pages → `publish_date='None'` → the strict ADR-0015 freshness gate dropped all 29 as undated; fixed by stamping the feed entry's outlet-authoritative pubDate as `publish_date` fallback. **Verified live post-deploy: BBC session tot=28 ok=21 (was ~1 article/day), 21 events published to RabbitMQ, Curator consuming.** Backfill re-applied (22 outlets with feeds). Ollama-reconnect systemd unit confirmed already in place + working. New `make soak-report` target. Soak clock starts now — next scheduled cycle is the first with RSS active fleet-wide.*
> *2026-06-11: **ADR-0008 — Editorial service (design accepted)** — daily per-theme editorial persona, separate service outside Curator, provider-pluggable LLM. Live bake-off on real corpus data: gemma4 12B = Spanish quality floor; 4B-class fails. Dev local (Mac), prod DeepSeek until the 16 GB droplet upgrade, then local.*
> *2026-06-11: **Curator ADR-0026 — embeddings offloaded to Hostinger.** The ADR-0025 concurrency fix was capped by the real bottleneck: DO's CPU-only Ollama embedded bge-m3 in ~15s under scrape contention, serialized → ~4/min pipeline ceiling → permanent ~3,700-msg queue / ~10h Reader lag. Pointed prod embeddings at the Hostinger box (82.112.250.139:32768, idle 16GB, firewalled to the droplet) running the SAME bge-m3 → ~2.2s warm, ~27/min, **zero re-embed / zero clustering break** (same 1024-dim space). Backlog drains in ~2.3h then stays real-time. One env var in docker-compose.do.yml; no migration.*
> *2026-06-11: **Curator ADR-0025 — concurrent pipeline + intake age gate.** Root cause of "old headlines pinned / new articles not showing": `prefetch_count=1` serialized the whole pipeline behind the 8–17s enrich LLM call → ~4.3 articles/min vs ~4–6k/day inflow → permanent ~3.5k-message queue ≈ 9–13h Reader latency. Fix: prefetch 8 + concurrent enrich, with `_embed_sem(1)` protecting CPU Ollama and `_cluster_lock` preventing duplicate-event seeding. Plus a 48h `scraped_at` intake gate (`max_article_age_hours`) that acks-and-drops stale messages before any LLM spend. Verified locally on real articles: **~70/min (16×)**, zero Ollama timeouts, concurrent same-story articles attach to one event, 60h-old message dropped at intake. Committed, pending deploy.*
> *2026-06-11: **Breaking-news fast lane, Phases 1+2 implemented** (Messor ADR-0017 + Curator ADR-0024) — 5-min RSS-only pulse over 10 flagged outlets (`outlets.pulse`, Backoffice switch), per-article AMQP priority 9, Curator consume queue declared `x-max-priority=10` (graceful fallback until the existing prod queue is drained+deleted — see ADR-0024 §migration), cluster-velocity breaking detector (≥2 pulse outlets within 60 min → `events.breaking_at`, demotes after 2h). Tested locally: detector positive/negative/re-fire/TTL cases, real AMQP priority ordering, feed_only skip path. **Deployed 2026-06-11 ~20:30 UTC**: migration 014 applied, 10 pulse outlets seeded, pulse thread live ("⚡ Pulse lane active"). Queue migrated zero-loss via `infra/scripts/migrate_priority_queue.py` (worker stopped → 3,248 msgs shoveled to tmp → queue recreated `x-max-priority=10` + rebound → msgs shoveled back → worker restarted; the planned "wait for drain" was impossible — backlog drains ~1 msg/min ≈ 2 days). Note: an orphaned legacy `articles-scraped` queue (346 msgs, zero consumers, pre-ADR-0004) exists — delete when convenient. Phases 3+4 (breaking synthesis persona + Reader rail) agreed, not started.*

---

## TL;DR

The full v0 pipeline runs end-to-end on real infrastructure:
**Messor (harvest) → RabbitMQ → Curator (enrich + cluster + synthesize) → Reader (pages)**.

- **Reader:** https://inkbytes.org
- **Backoffice:** https://admin.inkbytes.org
- **Server:** DigitalOcean Droplet `67.205.136.61` · Docker + Traefik

---

## Live pipeline numbers (2026-06-07)

| Metric | Value |
|---|---|
| Total articles | 7,591 |
| Enriched | 7,078 (93%) |
| Events | 413 |
| Published pages | 413 |
| Active outlets | 22 of 34 |
| LLM — enrich | DeepSeek `deepseek-chat` |
| LLM — synthesize | DeepSeek `deepseek-reasoner` |
| Embeddings | Ollama `bge-m3` (1024-dim) |
| Cost projection | ~$5–10 / 1000 articles |
| Scraping schedule | 12×/day (~120 min interval) |

---

## Service state (production — DigitalOcean `67.205.136.61`)

| Service | Container | URL / Port | State |
|---|---|---|---|
| Reader (Next.js) | `inkbytes-reader` | `:18050` → `inkbytes.org` | ✅ healthy |
| Backoffice (nginx+php-fpm) | `inkbytes-backoffice` | `:18051` → `admin.inkbytes.org` | ✅ up |
| Curator API | `inkbytes-curator-api` | `:8060` (internal) | ✅ healthy |
| Curator Worker | `inkbytes-curator-worker` | internal | ✅ healthy |
| Messor harvester | `inkbytes-messor` | `:8050` (internal) | ✅ running |
| Postgres + pgvector | `inkbytes-postgres` | `:5432` (internal) | ✅ healthy |
| RabbitMQ | `inkbytes-rabbitmq` | `:5672` / mgmt `:15683` | ✅ healthy |
| MinIO (S3 stand-in) | `inkbytes-minio` | `:9000` / console `:9030` | ✅ healthy |
| Ollama (shared host) | `ollama` | `:11434` (host-network) | ✅ bge-m3 loaded |
| Traefik | host-network | `:80/:443` | ✅ TLS via Let's Encrypt |

### Memory limits (infra/docker-compose.prod.yml)

| Container | Limit | Typical usage |
|---|---|---|
| `inkbytes-messor` | 6 GB | 2–3 GB mid-scrape, ~150 MB idle |
| `inkbytes-curator-worker` | **1.5 GB** | ~200 MB idle / ~400 MB peak (IllustrateSkill — 1× Chromium for YouTube only, serialised via Semaphore(1); Bing image fetcher parked ADR-0014); `shm_size:'256m'` + `seccomp:unconfined` required to prevent Chromium SIGTRAP on Docker |
| `inkbytes-reader` | 512 MB | ~65 MB |
| `inkbytes-backoffice` | 512 MB | ~115 MB |
| `inkbytes-curator-api` | 512 MB | ~110 MB |
| `ollama` | 3 GB | ~1.6 GB (bge-m3) |

---

## Active outlets (65 — all active as of 2026-06-11)

All outlets in `public.outlets` are active. 22 have a droplet-verified `feed_url`
(RSS-first); the rest use newspaper3k homepage crawl. Check live health with
`make soak-report`.

### Outlets with verified RSS feeds (backfilled 2026-06-11)

`bbc · aljazeera · eldinerodr · npr · theguardian · latimes · gizmodo · clarin ·
cnbc · wired · foxbusiness · bloomberg · financialtimes · elfinancierolatam ·
eluniversalmx · infobae · lanacionar · theeconomist · semana · techcrunch · wsj ·
eltiempoco`

### No working feed found (probed 2026-06-11 — stay on newspaper3k)

| Outlet | Probe result |
|---|---|
| apnews | rsshub proxy 403; no official RSS |
| cnn | rss.cnn.com discontinued |
| reuters | feeds.reuters.com discontinued |
| elespectador | 404 on all known paths (incl. Arc outboundfeeds) |
| animalpolitico | 404 on /feed, /rss, /rss.xml |
| mileniomx | 403 (bot-blocked) on /rss |

Note: paywalled outlets (bloomberg/wsj/economist/FT) have working *feeds* but
article-page fetches may still hit the paywall — watch their parse_pct in the
soak report before judging.

---

## v0 Definition of Done

- [x] Curator runs end-to-end on real LLM
- [x] At least one outlet returned ≥ 5 articles via Messor
- [x] First event pages in `pages` table (413 multi-source pages)
- [x] Reader renders events at production URL
- [x] Server running docker-compose.prod.yaml at `inkbytes.org`
- [x] Continuous 12×/day scheduled cycles with per-outlet session tracking
- [x] Synthesis cost cap (ADR-0015) — max 15 articles / 2 per outlet; 9× token reduction deployed 2026-06-08
- [x] Duplicate enrichment fast-path (ADR-0015) — unchanged articles skip LLM; queue drains in ~2h not ~21d, deployed 2026-06-08
- [x] Stable `content_hash` (ADR-0018) — fixes the ADR-0015 fast-path that never fired (raw hash churned on newspaper3k noise → 0 SKIP / 176 ENRICH, ~$780 / 13-day drain); now hashes a normalized lede prefix. Tested locally, pending deploy.
- [x] Persistent Messor staging volume (ADR-0012) — `inkbytes_inkbytes-messor-scrapes` pre-seeded; no more restart floods, deployed 2026-06-08
- [x] Per-RUN staging files (Messor ADR-0014) — fixes the per-day file that re-published in full every cycle; ADR-0016 migration teardown had wiped the dedup volume → 105 143-msg backlog (purged 2026-06-09). Volume mount intact (survives restart); per-run files make any future wipe survivable. Tested locally, pending deploy.
- [ ] 24h of green scheduled cycles + first paying user invited
- [ ] Switch to `inkbytes.news` domain (DNS A records → `67.205.136.61`)
- [ ] GitHub Actions CI/CD (secrets: `DEPLOY_HOST`, `DEPLOY_USER`, `DEPLOY_KEY`)

---

## Monitoring commands (from repo root)

```bash
make status           # snapshot: all containers + memory table
make watch            # live stats every 5s
make logs             # follow ALL service logs
make logs-messor      # follow Messor (scraping progress)
make logs-curator     # follow Curator worker (enrich/cluster/synth)
make logs-backoffice  # follow Backoffice (Laravel)
make health           # HTTP status of reader + admin
make shell            # SSH into server
make shell-db         # psql on production Postgres
make shell-curator    # bash in curator-api container
```

---

## Open items / next steps

### P0 — Blocking on first paying user

1. **`inkbytes.news` domain** — add A records → `67.205.136.61`; update `READER_DOMAIN`/`ADMIN_DOMAIN` in `infra/.env` on DO Droplet; update Traefik config.
2. ✅ **Ollama reconnect on reboot** (verified 2026-06-11) — `ollama-inkbytes-network.service` systemd unit on the droplet (enabled, oneshot, waits up to 60s for the `inkbytes_inkbytes-internal` network then connects `infra-ollama`). `deploy.sh` also reconnects at every deploy. Connectivity verified from inside `inkbytes-curator-worker` (HTTP 200 on `/api/tags`).
3. **Phase 3 / Stripe** — subscriber gating blocked on pricing decisions. See `docs/product.md`.
4. **24h green soak** — let 4 scheduled cycles complete; verify Scrape Results shows outlets at >80% parse success.

### P1 — Scraping quality

5. ✅ **RSS/Atom-first harvesting** (Messor ADR-0013, 2026-06-10) — `feed_url` per outlet; `FeedScraper` with 48h freshness gate; newspaper3k fallback when feed absent/fails. BBC, Al Jazeera, Reuters, wired, foxbusiness, animalpolitico, eldinerodr + 3 LATAM dailies seeded. **Re-enable disabled outlets in Backoffice.**
6. ✅ **Per-outlet `feed_url` in DB** — Curator migration 012 + Laravel migration 000001 (both deployed).
7. ✅ **Per-outlet `min_word_count`** (Messor ADR-0016, 2026-06-10) — overrides global 40-word floor per outlet (BBC/Reuters/LATAM dailies set to 25).
8. **trafilatura fallback parser** — when newspaper3k fails, try trafilatura. Better for modern JS sites.
9. **Per-outlet rate-limit / custom headers** — follow the ADR-0016 pattern: one DB column + SCRAPER_FIELDS entry + Pydantic field.

### P2 — Reader

9. **Reader R4** — global perf/SEO pass. Lighthouse score, sitemap.xml.
10. **Language filter on feed** — Spanish content stays in Spanish; Reader shows language badge.
11. **Media Tier 1** ✅ 2026-06-07 — Messor passively extracts `og:image`/`top_image` and YouTube embeds; stored in `articles.lead_image`/`video_url`; rolled up to event level in API; Reader LeadCard, SecondaryCard, and event detail hero show cover image (ADR-0010).
    - ✅ 2026-06-08 — **Lead-image hotlink guard** (Curator ADR-0019): `MediaValidator` probes each og:image at ingest with the browser `Sec-Fetch-Dest:image`+`Sec-Fetch-Site:cross-site` fingerprint and NULLs URLs that hotlink-redirect to a placeholder pixel / 404 / serve an error page / a <512B pixel, so the `/events` rollup falls back to another source. Deployed + one-time backfill applied (`scripts/backfill_lead_images.py`). Toggle `application.validate_lead_images` (default true).
12. **Media Tier 2** ✅ 2026-06-07 — `IllustrateSkill` output (`pages.media_rail`) surfaced end-to-end. Curator API exposes `media_rail`; `MediaRailItem` typed in Reader. Event page: collapsed drawer in the action bar (streaming icon + pulse rings alongside Share button) — expands to **video chips only** on click. `MediaRailDrawer` client component owns toggle state (ADR-R-0003, ADR-R-0004).
    - ✅ 2026-06-08 — **Media rail video-only** (ADR-0014 / ADR-R-0006): Bing image fetcher parked; `media_rail` stores YouTube videos only. Eliminates off-topic editorial/product photos. Existing image items silently skipped at render time — no migration needed.
15. **Entities graph on mobile** ✅ 2026-06-07 — Force-directed SVG graph now renders on all screen sizes (was replaced by a pill list on mobile). Single responsive grid (`grid-cols-1 md:grid-cols-[1fr_320px]`); `touchAction:none` on SVG so finger-drag moves nodes without triggering page scroll (ADR-R-0004).
13. **PWA + bottom nav + share** ✅ 2026-06-07 — Web App Manifest, bottom nav (News/Search/Entities/About), Web Share API share button on event pages (ADR-R-0001). TODO: generate 192/512px PNG icons for Android install banner.
14. **Brand logo mark** ✅ 2026-06-07 — Real brand SVG (`InkBytes-Logo-White-cropped.svg`) wired into header (`<LogoMark>` component, `currentColor`) and favicon (`icon.svg` dark bg) replacing placeholder (ADR-R-0003).
14. **PNG app icons** — generate `/public/icon-192.png` and `/public/icon-512.png` from `icon.svg` for full Android PWA install prompt (`inkscape --export-type=png --export-width=192`).

### P2 — Event lifecycle (Sprint 2)

14. **Story arc archive (Curator ADR-0013)** — When a published event's `last_updated_at` is older than 7 days, mark it `concluded` and write a `story_arcs` record with `arc_article_ids TEXT[]` (ordered pointers into `articles.embedding`). Enables future "concluded story" UI badge, arc-similarity recommendation, and RAG context injection. Migration 010 + `--conclude-stories` command + `conclude_after_days` config.
15. **Stale article filter (Curator ADR-0012)** — Gate in Curator's `_handle_event()`: if `article.published_at > max_article_age_days (default 7)` AND no active cluster matches, drop without ENRICH/EMBED. Prevents orphan events from old re-featured homepage content. Depends on `find_nearest_active_event()` probe query + `max_article_age_days` config. *(Note: Messor ADR-0012 is the separate staging-volume fix — different service, same number is fine.)*

### P2 — Editorial service (Sprint 2)

16. **Editorial service (ADR-0008, design accepted 2026-06-11)** — daily per-theme
    editorial column by a named persona, **separate service outside Curator**.
    Provider-pluggable LLM (`ollama | deepseek | anthropic`, ADR-0003 pattern).
    Bake-off result: **gemma4 12B is the quality floor for Spanish** — 4B-class
    fails (gender errors, anglicisms, invented facts). Sequencing: dev = local
    gemma4 on the Mac; prod launch = DeepSeek (<$0.05/day — droplet can't host
    12B); prod local after the 8→16 GB droplet upgrade. Themes from
    `articles.theme` (8 verticals); `events.topic` is empty — don't use it.
    See [`docs/adr/0008-editorial-service-llm-selection.md`](./adr/0008-editorial-service-llm-selection.md).

### P2 — Infrastructure (Sprint 2)

11. **Object store consolidation + embedding durability (Curator ADR-0016)** — Two separate objects stores in prod (MinIO internal + DO Spaces for Messor) is an accident of history. Sprint 2: remove `inkbytes-minio` from prod stack; point all services (Curator, Backoffice) at DO Spaces. Add daily `pg_dump` to DO Spaces (`backups/postgres/YYYY-MM-DD.sql.gz`, 28-day retention) — reduces embedding recovery from ~18h re-embed to ~10 min restore. pgvector stays; re-evaluate at 100k articles (~2 months out).

### Marketing site (inkbytes.news)

- ✅ **`inkbytes` WordPress theme committed** to the monorepo at
  `docs/marketing/inkbytes-theme/` (2026-06-14) — it's the live homepage of
  `inkbytes.news`. Deployed **manually** to the Hostinger box (`docker cp` →
  activate via DB), not in CI; revert = stock `twentytwentyfive`. See memory
  `hostinger-wordpress-hardening` + the theme README for deploy/activate steps.
- **Phase-1 marketing site v2** (`docs/marketing/site/`) — a richer multi-page
  static site (about/methodology/pricing/contact) on the InkBytes Design System
  is in progress in a separate workstream; not yet committed or deployed (depends
  on a `_ds/` bundle not yet in the tree). Decision pending: keep under
  `docs/marketing/` or extract to its own repo.
- **Outstanding host-level exposures on the Hostinger box** (flagged, awaiting decision):
  open-webui world-exposed on `:8080` (host-network) + `:32769`; SSH `PermitRootLogin yes`
  + `PasswordAuthentication yes`. (Ollama `:32768` is correctly firewalled.)

### P3 — Infrastructure

12. **GitHub Actions CI/CD** — auto-build and deploy on `git push master`. Set secrets: `DEPLOY_HOST`, `DEPLOY_USER`, `DEPLOY_KEY`.
13. **Curator config default** — change `embeddings_base_url` default to `http://ollama:11434/v1` (currently `localhost:11434` breaks after `migrate:fresh`).
14. **Approach B entity embeddings** — event-level pgvector for related-events at >1000 events scale.
15. **Reader traffic analytics in Backoffice** ("Reader Hits") — surface per-request hits to the Reader (top events, by country, over time) in the Backoffice. Design note (incl. GDPR/PII handling — salted IP hash, GeoIP-then-discard): [`docs/reader-traffic-analytics.md`](./reader-traffic-analytics.md). Leading approach: Reader middleware → RabbitMQ `reader.hit.v1` → aggregate table. **Design only, not built.**

---

## Repository

- **Canonical remote:** https://github.com/pragmatalabs/InkBytes (`master`)
- **Registry:** GitHub Container Registry `ghcr.io/pragmatalabs` (free; replaced DO registry)
- **Secrets:** never committed — set via `infra/.env` on server (see `docs/deployment-secrets.md`)
