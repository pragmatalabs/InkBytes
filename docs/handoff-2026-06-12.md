# Session handoff → 2026-06-12

> *Written 2026-06-11 late evening (EDT). Pick this up first tomorrow. Live truth is always [`STATUS.md`](./STATUS.md); this is the "what I was mid-flight on" note.*

## Where things stand right now

The pipeline is **healthy and draining**, not stalled. Everything below is deployed to prod (DO `67.205.136.61`) and pushed to `origin/master`.

- **Fresh start done** — cut the stale tail (events not updated in 48h): 27,689→17,210 articles, 11,044→2,597 events, 1,088→876 pages. Outlets config preserved. Backup at `/opt/inkbytes/backups/pre-freshstart-20260612-021538.dump` (170 MB).
- **Embeddings offloaded to Hostinger** (Curator ADR-0026) — DO's CPU Ollama embedded bge-m3 in ~15s under contention → ~4/min pipeline ceiling. Now points at `82.112.250.139:32768` (same bge-m3, ~2.2s warm, zero re-embed). Pipeline went net-zero → net-positive (~+5/min).
- **Queue draining** — was ~3,700 at handoff, drains ~5-10/min, **should be near-empty by morning**. The drain watcher (below) will confirm.
- **Breaking-news lane (Phases 1+2) live** — 5-min pulse over 10 outlets, priority queue, cluster-velocity detector. **No real breaking event flagged yet** (needs ≥2 pulse outlets on one story within 60 min). Watcher armed.
- **Synthesis storm fixed** — persistent watermark (migration 015), no more post-deploy storms.

### Two background watchers were running at handoff (they end with the session)
- **Queue drain** — reported depth every 30 min, exits when <100.
- **First breaking-news flag** — pings on the first real `breaking_at`.
Re-arm them tomorrow if you want them: a `psql` poll on queue depth, and `SELECT id, breaking_at FROM events WHERE breaking_at IS NOT NULL`.

## First moves tomorrow (in order)

1. **Check the soak**: `make soak-report` + confirm the queue cleared overnight + look for the first breaking flag. This is the green light for first paying user (the standing P0).
2. **REINDEX the embedding index** (task #1) — free ~180 MB (disk at 89%) + better recall. `REINDEX INDEX CONCURRENTLY idx_articles_embedding;` Zero risk, do anytime.
3. **Raise `_embed_sem` 1→2-3** (task #2) — the real throughput lever (embed is the serial bottleneck, NOT clustering which is 12ms). Test 2-3 concurrent embeds on Hostinger first.

## The task queue (9 items, all carry full context)

| # | Task | Notes |
|---|---|---|
| 1 | REINDEX embedding index | free win, do first |
| 2 | Raise `_embed_sem` 1→2-3 | throughput; test Hostinger capacity |
| 3 | ivfflat → HNSW | recall/quality; after soak |
| 4 | Fix stalled cost persistence | `model_usage` 0 rows/24h despite COST logs |
| 5 | model_usage retention | low priority |
| 6 | **[Epic] Make taxonomy navigable** | Julian confirmed ALL THREE surfaces |
| 6a (#7) | API: `?theme=` filter, `/topics/trending`, `?category=` | **build first — 6b/6c depend on it** |
| 6b (#8) | Reader: theme filter chips + topic trending | blocked by 6a |
| 6c (#9) | Backoffice: theme/category/topic filters | blocked by 6a |

Taxonomy reality (for the epic): `theme` = 8 clean values (the filter facet), `topic` = free-text LLM labels incl. junk like "Browser Error" (labels/trending, light cleanup), `article_category` = raw outlet sections (ops filter). They're already shown as labels; the gap is *navigation/filtering* — that's what makes the idle indexes earn their keep.

## Standing context / gotchas
- **Hostinger box** `82.112.250.139` (rebuilt `srv1521854`, 16 GB): SSH `~/.ssh/id_ed25519` (NOT galvanic_id). Runs Ollama on :32768 (firewalled to the droplet only), now serves prod embeddings + will host the Editorial 12B. ⚠️ open-webui on :32769 still world-exposed — Julian to decide.
- **Editorial service** (ADR-0008): designed, not built; runs gemma4:12b on the Hostinger box.
- **Reader IP-hits analytics**: design-only note at `docs/reader-traffic-analytics.md`, P3.
- **Pending branch**: `chore/remove-dead-process-found-articles` (dead-code cleanup) still unmerged.
- Deploy = SSH to droplet, `git pull && bash infra/deploy.sh --build`. Embeddings URL lives in `infra/.env` on the server (overrides the compose default).
