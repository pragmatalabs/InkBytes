# Handoff — 2026-06-13

> *Status: note · Owner: Julián (via Cowork/documentor-agent) · Last updated: 2026-06-13*
> Memoria de los cambios hechos en esta sesión, para retomar después.

## Qué se hizo

### 1. Suite de documentación a nivel de código — `docs/code/` (NUEVO)
Generada con el skill `documentor-agent` en modo FULL SCAN (todo el monorepo). Es la capa **de código** (módulos, APIs, schema, reglas de negocio, onboarding); complementa — no duplica — la capa **arquitectónica** existente en `docs/architecture/` (architect-documentor) ni el `docs/README.md` raíz.

11 archivos creados:

```
docs/code/
├── README.md                      ← índice + mapa de servicios + diagrama de contexto C4 L1
├── technical/
│   ├── stack.md                   ← stack por servicio, versiones, dependencias
│   ├── api-reference.md           ← 18 endpoints (Curator 10, Messor 4, Backoffice, Reader) + contratos RabbitMQ
│   ├── database-schema.md         ← 8 tablas public + 7 backoffice, ER Mermaid, pgvector
│   └── configuration.md           ← env vars + config de los 4 servicios + regla de regiones
├── functional/
│   ├── features.md                ← funcionalidades (lector / operador / pipeline)
│   ├── business-rules.md          ← 22 reglas de negocio con clase/método + ADR
│   └── workflows.md               ← 6 diagramas Mermaid (cosecha→publicación, clustering, moderación, chat…)
└── onboarding/
    ├── getting-started.md         ← setup local paso a paso
    ├── development-guide.md       ← convenciones + anti-patrones
    └── glossary.md                ← ~35 términos del dominio
```

Verificado contra el código fuente: las 10 rutas de Curator (`core/api_server.py`), las 4 de Messor (`api/routers/scrape.py`) y los scripts `orchestrator/scripts/{up,down,dev-pipeline}.sh` existen tal cual se documentan. Diagramas Mermaid con fences balanceados; sin placeholders.

### 2. Landing page de producto — `docs/marketing/index.html` (NUEVO)
One-page para clientes generales, HTML self-contained, on-brand (paleta real del Reader `globals.css`: `--accent #1a1a2e`, `--accent-dot #e05c5c`, `--bg #fafaf9`; tipografías Inter + Newsreader; logo auténtico nib→pixels de `Reader/.../components/logo.tsx`). Secciones: hero "One page worth twenty" + mock de evento, problema (fragmentado/ruidoso/sesgado), pipeline de 5 pasos, tabla comparativa vs. competidores, features, pricing Free vs **$9/mo**, audiencias, CTA. Mensajes tomados de `docs/product.md`.

## Pendientes / próximos pasos sugeridos
- **Landing**: generar variante en **español** (mercado v0 LATAM); conectar los CTA a un flujo real de signup/checkout (hoy son `#`).
- **Docs**: opcional enlazar `docs/code/` desde el `docs/README.md` raíz; correr `architect-documentor` para reconciliar las vistas C4/deployment.
- **Hallazgos de revisión manual** (detectados al documentar):
  - `Messor/apps/scraper/api/utils/api_security.py` tiene una API key hardcodeada (`'ghyuhjbjhjhhjhjhj'`) en código muerto no cableado → limpiar.
  - `Reader/apps/web/proxy.ts` (gate de auth) está **deshabilitado en v0** (devuelve `NextResponse.next()`) → confirmar antes de cobrar.
  - ADRs con numeración duplicada en Curator (`0016`, `0017`, `0028` aparecen dos veces) → reconciliar `Curator/docs/adr/`.

## Estado git
Cambios solo en archivos nuevos bajo `docs/code/` y `docs/marketing/`. **No commiteado / no pusheado** — pendiente de tu instrucción (Regla 2 del dev-workflow).
