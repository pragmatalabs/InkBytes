# Messor scraper — top-level runtime dependencies.
# Transitives are resolved by pip. For reproducible builds use requirements-lock.txt.
#
# Edited 2026-06-02 (INK-2):
#   + added lxml_html_clean (required by newspaper3k on lxml>=5.2)
#   - dropped fake deps: common, logger, restclient
#   - dropped pydantic_core (v2-internal; conflicts with pydantic v1)
#   - dropped system tools: virtualenv*, virtualenvwrapper
#   - dropped unused: pandas, numpy, pythainlp, jieba (let newspaper3k pull what it needs)
#   - deduplicated (file was duplicated end-to-end)

# --- Shared kernel (real Python package, see packages/inkbytes) ---
# Installed via `pip install -e ../../packages/inkbytes` (INK-1).
# Replaces the old fake `common==0.1.2` and the inkbytes symlink.
-e ../../packages/inkbytes

# --- Web scraping ---
newspaper3k==0.2.8
lxml_html_clean>=0.4.1           # newspaper3k requirement on lxml>=5.2
beautifulsoup4>=4.12.3
requests>=2.31.0
feedparser>=6.0.11
feedfinder2>=0.0.4
tldextract>=5.1.1
langdetect>=1.0.9
nltk>=3.8.1

# --- API / config / messaging ---
# fastapi pinned to last release compatible with pydantic v1 (we're on v1
# until INK / Sprint-2 migrates the BaseModels). Bump together when ready.
fastapi==0.99.1
uvicorn[standard]>=0.27.0
pydantic>=1.10.14,<2            # v1.x; migration to v2 is Sprint-2 work
PyYAML>=6.0.1
python-dotenv>=1.0.0
pika>=1.3.2

# --- Cloud / object store ---
boto3>=1.34.44

# --- Data processing (required by packages/inkbytes shared lib) ---
numpy>=1.26.0
pandas>=2.2.0

# --- Dev convenience ---
aiofiles>=24.1.0
